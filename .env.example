# Credenciais de deploy — consumidas pelos scripts/deploy_*.py (acesso SSH ao VPS de produção).
# Copie este arquivo para `.env` na raiz do repo e preencha RELM_VPS_PASS.
# O `.env` real é gitignorado; NUNCA commite a senha.
RELM_VPS_HOST=177.153.62.248
RELM_VPS_USER=root
RELM_VPS_PASS=
